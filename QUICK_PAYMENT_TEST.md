# Быстрое тестирование оплаты

## Вариант 1: Тест через API (без БД - только проверка создания платежа)

Для тестирования только API Т-банка (без сохранения в БД), можно использовать прямой вызов:

```bash
# Получить тарифы (работает без БД)
curl http://localhost:3000/api/payments/pricing

# Создать платеж (требует БД для сохранения)
curl -X POST http://localhost:3000/api/payments/create \
  -H "Content-Type: application/json" \
  -d '{"deviceId":"test-123","tierId":"tier1"}'
```

## Вариант 2: Полный тест с БД

### 1. Настройте БД в `.env`:

```env
MYSQL_HOST=localhost
MYSQL_USER=root
MYSQL_PASSWORD=ваш_пароль
MYSQL_DATABASE=vissreader
MYSQL_PORT=3306
```

### 2. Создайте базу данных:

```sql
CREATE DATABASE IF NOT EXISTS vissreader;
```

### 3. Запустите сервер:

```bash
npm start
```

Сервер автоматически создаст все необходимые таблицы при первом запуске.

### 4. Запустите тест оплаты:

```bash
# Тест tier1 (1000 токенов за 300 ₽)
npm run test:payment:tier1

# Тест tier2 (2000 токенов за 549 ₽) - рекомендуется
npm run test:payment:tier2

# Тест tier3 (4000 токенов за 999 ₽)
npm run test:payment:tier3

# Или с указанием тарифа
node test-payment-simple.js tier1
```

### 5. После создания платежа:

1. Скопируйте **Payment URL** из вывода
2. Откройте его в браузере
3. Выполните тестовый платеж
4. Проверьте статус:

```bash
# Укажите Payment ID из вывода
node test-check-status.js payment_1234567890_abc123

# Или через браузер
http://localhost:3000/api/payments/status/payment_1234567890_abc123
```

## Что тестируется

✅ Создание платежа через Т-банк API  
✅ Получение Payment URL  
✅ Проверка статуса платежа  
✅ Начисление токенов после успешной оплаты (если БД настроена)

## Пример вывода

```
💳 Тестирование оплаты через Т-банк API

Base URL: http://localhost:3000
Test Device ID: test-device-1767858002634
Tier ID: tier1

📋 Шаг 1: Получение тарифов...

✅ Выбран тариф: 1000 токенов за 300 ₽

💳 Шаг 3: Создание платежа...

✅ Платеж успешно создан!

======================================================================
📝 ДЕТАЛИ ПЛАТЕЖА:
======================================================================
Payment ID: payment_1767858002634_abc123
Order ID: payment_1767858002634_abc123
Сумма: 300 ₽
Токены: 1000
Статус: processing
======================================================================

🔗 PAYMENT URL (скопируйте и откройте в браузере):
======================================================================
https://securepayments.tbank.ru/...
======================================================================
```

## Проверка статуса

После оплаты проверьте статус:

```bash
node test-check-status.js payment_1767858002634_abc123
```

Или через API:

```bash
curl http://localhost:3000/api/payments/status/payment_1767858002634_abc123
```

## Troubleshooting

### Ошибка: "Access denied for user"

**Решение:** Настройте переменные окружения для MySQL в `.env`

### Ошибка: "Т-банк конфигурация не настроена"

**Решение:** Убедитесь, что в `.env` есть:
```env
TBANK_TERMINAL_KEY=1703150935625DEMO
TBANK_PASSWORD=xcbixwo8gsjibu6u
```

### Ошибка: "Failed to create payment"

**Решение:** 
- Проверьте логи сервера
- Убедитесь, что Т-банк API доступен
- Проверьте правильность TerminalKey и Password

### Payment URL не открывается

**Решение:**
- Проверьте, что URL скопирован полностью
- Убедитесь, что используется HTTPS
- Проверьте, что Т-банк API доступен

## Следующие шаги

После успешного тестирования:
1. Получите production данные от Т-банка
2. Настройте production переменные окружения
3. Настройте webhook URL на production сервере
4. Протестируйте на production

