# Viss Reader Backend

Backend для Android-приложения AI Book Reader. Генерирует AI-иллюстрации для книг через Perplexity API.

## 🚀 Технологии

- **Node.js 20+**
- **Express.js** - веб-сервер
- **Perplexity AI API** - генерация изображений
- **Joi** - валидация данных
- **CORS** - поддержка Android приложения

## 📋 Требования

- Node.js 20 или выше
- Perplexity AI API Pro ключ

## 🔧 Установка

1. Клонируйте репозиторий или скопируйте файлы
2. Установите зависимости:

```bash
npm install
```

3. Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

4. Добавьте ваш Perplexity API ключ в `.env`:

```
PERPLEXITY_API_KEY=your_api_key_here
```

## 🏃 Запуск

### Локальная разработка

```bash
npm run dev
```

Сервер запустится на `http://localhost:3000`

### Production

```bash
npm start
```

## 📡 API Endpoints

### POST `/api/generate-image`

Генерирует AI-иллюстрацию для фрагмента текста из книги.

**Request Body:**
```json
{
  "bookTitle": "Война и мир",
  "author": "Лев Толстой",
  "textChunk": "Он стоял на балконе, глядя на закат..."
}
```

**Response (Success):**
```json
{
  "success": true,
  "imageUrl": "https://cdn.perplexity.ai/...",
  "promptUsed": "A detailed artistic illustration..."
}
```

**Response (Error):**
```json
{
  "success": false,
  "error": "Error message"
}
```

**Status Codes:**
- `200` - Успешная генерация
- `400` - Ошибка валидации данных
- `429` - Превышен лимит запросов
- `500` - Ошибка сервера или API

### GET `/health`

Проверка работоспособности сервера.

**Response:**
```json
{
  "status": "ok",
  "timestamp": "2024-01-01T12:00:00.000Z"
}
```

## 🚂 Деплой на Railway

1. Создайте проект на [Railway](https://railway.app)
2. Подключите ваш GitHub репозиторий или загрузите код
3. Добавьте переменную окружения:
   - `PERPLEXITY_API_KEY` - ваш Perplexity API ключ
4. Railway автоматически определит Node.js проект и запустит `npm start`

### Railway Configuration

Railway автоматически:
- Определит Node.js проект
- Установит зависимости (`npm install`)
- Запустит сервер через `npm start`
- Установит переменную `PORT` автоматически

## 📝 Валидация данных

- `bookTitle`: строка, максимум 100 символов, обязательное
- `author`: строка, максимум 50 символов, обязательное
- `textChunk`: строка, максимум 500 слов, обязательное

## ⚠️ Важные замечания

1. **Perplexity API**: Убедитесь, что у вас есть Pro ключ с доступом к image generation
2. **Timeout**: Запросы к API имеют timeout 30 секунд
3. **Rate Limits**: Приложение обрабатывает ошибки rate limiting (429)
4. **CORS**: Настроен для работы с Android приложением (разрешены все источники)

## 🐛 Отладка

Логи запросов выводятся в консоль. Для более детального логирования можно добавить библиотеку типа `winston` или `pino`.

## 📄 Лицензия

ISC

